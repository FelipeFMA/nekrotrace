{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "main",
  "description": "Main capability: allow core default and event listen",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen"
  ]
}
